# Neovim Config

This `.docker` directory contains Neovim configuration for solo development **inside the Docker container**.  

These files were added here for convenience in this personal project.  
In general, mixing container/editor configs within a repository is not advised.

